# Core dependencies (synced with pyproject.toml). Requires Python 3.10+.
openai>=1.0.0
anthropic>=0.18.0
transformers>=4.30.0
huggingface-hub>=0.16.0
pyyaml>=6.0

# NLP (synced with pyproject.toml [nlp] optional dependencies)
nltk>=3.8.0
spacy>=3.5.0
scikit-learn>=1.2.0
gensim>=4.3.0

# Visualization (synced with pyproject.toml [visualization] optional dependencies)
matplotlib>=3.7.0
pandas>=2.0.0
seaborn>=0.12.0

# Development tools (synced with pyproject.toml [dev] optional dependencies)
pytest>=7.3.0
pytest-cov>=4.1.0
pytest-asyncio>=0.21.0
ruff>=0.1.0
mypy>=1.5.0
black>=23.0.0
isort>=5.12.0
pre-commit>=3.3.0
types-PyYAML>=6.0.12.20240311

# Documentation (synced with pyproject.toml [docs] optional dependencies)
sphinx>=7.0.0
sphinx-autodoc-typehints>=1.24.0
furo>=2023.9.10
sphinx-autobuild>=2021.3.14

# NOTE: This file is kept for compatibility with tools that expect requirements.txt.
# The canonical source of truth for dependencies is pyproject.toml.
# When updating dependencies, always update pyproject.toml first, then sync this file.
